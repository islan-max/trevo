# DEP-01: pytest, ruff, bandit etc. viviam em requirements.txt — a Vercel
# instala esse arquivo para a função serverless, então todo esse ferramental
# de teste/lint ia junto no bundle de produção. Isolado aqui; CI e
# desenvolvimento local instalam este arquivo, que já inclui o de produção.
-r requirements.txt

pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=5.0
# Usado por tests/unit/test_clock.py e tests/integration/test_cookie_session.py
# (renovação deslizante de sessão) para viajar no tempo sem esperar de verdade.
freezegun>=1.4
bandit>=1.7
ruff>=0.4
