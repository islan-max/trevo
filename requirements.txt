fastapi>=0.110
# ConfigDict (em vez de class Config, removida no Pydantic V3) usada em todo
# app/*/schemas.py.
pydantic>=2.9,<3
uvicorn[standard]>=0.27
psycopg2-binary>=2.9
supabase>=2.0
# DEP-06: PyJWT no lugar de python-jose (sem commits desde 2021, changelog
# de segurança parado). app/api/deps.py, app/oauth.py e app/core/security.py
# usam só HS256, então nenhuma extra de criptografia assimétrica é necessária.
pyjwt>=2.9,<3
# DEP-05: bcrypt direto no lugar de passlib (também sem commits recentes).
# O teto <4.1 existia só por causa da checagem de versão que a própria
# passlib fazia no bcrypt (AttributeError em bcrypt>=4.1); sem passlib no
# meio, nada mais impõe esse teto.
bcrypt>=4.0
python-multipart>=0.0.9
slowapi>=0.1.9
python-dotenv>=1.0
httpx>=0.27
# zoneinfo (app/shared/clock.py) usa o banco de fusos do sistema operacional
# quando existe, e cai neste pacote quando não existe. python:3.12-slim e o
# runtime Python da Vercel nem sempre trazem o tzdata do SO — sem isto,
# ZoneInfo("America/Sao_Paulo") levantaria ZoneInfoNotFoundError em produção.
tzdata>=2024.1
